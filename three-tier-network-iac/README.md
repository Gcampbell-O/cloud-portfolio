# IaC Rebuild — Three-Tier Network (Bicep)

🚧 Work in progress.

Rebuilding the [Three-Tier Sandbox Network](../three-tier-sandbox-network/) as declarative Infrastructure as Code, with a GitHub Actions pipeline to follow.

**Done so far:** VNet, subnets, NSGs, Azure Bastion, and the jump-box VM — all in `main.bicep`.
**Still to add:** storage account + private endpoint, RBAC, remote state, and the CI/CD pipeline.
